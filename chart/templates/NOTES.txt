Thank you for installing {{ .Chart.Name }} v{{ .Chart.Version }}.

Controller image: {{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}

{{- if not (or .Values.vcenter.existingSecret .Values.vcenter.host) }}

WARNING: No vCenter credentials supplied. Either set `vcenter.existingSecret`
to a Secret you manage yourself (for example, one rendered by
ExternalSecretsOperator), or populate `vcenter.host`, `vcenter.user`, and
`vcenter.password` and the chart will render an inline Secret for you.
{{- end }}

{{- if and (not .Values.vcenter.caBundle.configMapName) (not .Values.vcenter.tlsVerify) }}

Note: vCenter TLS verification is DISABLED. To enable, pick one:
  - Self-signed or private CA: create a ConfigMap with your CA bundle (PEM)
    and set `vcenter.caBundle.configMapName` + `vcenter.caBundle.key`.
  - Public CA (Let's Encrypt, DigiCert, etc.): set `vcenter.tlsVerify: true`.
{{- end }}

Verify the controller is running:

  kubectl -n {{ .Release.Namespace }} rollout status deploy/{{ include "gpu-node-vsphere-maintenance-controller.fullname" . }}
  kubectl -n {{ .Release.Namespace }} logs deploy/{{ include "gpu-node-vsphere-maintenance-controller.fullname" . }} -f

GPU-worker Node label (`{{ .Values.config.gpuNodeLabel }}`) identifies the
nodes this controller will drain when their ESXi host enters maintenance.
